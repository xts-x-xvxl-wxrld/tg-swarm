# Change Log

## 2026-06-01

- Added a reusable private-group evaluation packet at `wiki/plan/testing-group-mock-campaign.md` for testing-group rehearsals, including a realistic fictional campaign, grounded claims and forbidden claims, operator prompts, tester message scripts, business-logic checks, writing-quality rubric, and hard-fail conditions; also linked it from `wiki/plan/telegram-live-sandbox-day-plan.md` and `wiki/index.md` so the sandbox runbook can be reused for more realistic campaign-quality evaluations.

## 2026-05-31

- Added best-effort idempotency for ambiguous MTProto send retries: before retrying a retriable transport-style send failure, the messaging capability now checks recent outbound chat history for the exact just-sent message and treats a matching recent outbound post as delivered instead of sending it again, with focused MTProto tests covering recovery without duplicate retries.
- Reduced MTProto session-file lock contention in the live sandbox: `TelethonSessionManager` now supports a per-process session namespace with canonical-to-worker session cloning, worker-local session copies refresh from the canonical onboarding session when newer, stale cleanup removes both canonical and namespaced artifacts, and `bin/live-sandbox.ps1` now assigns non-`poll` roles their own MTProto session namespace so long-lived listener/executor workers stop sharing one Telethon SQLite session file.
- Fixed the persistent direct-send control-plane mismatch: the orchestrator prompt and compiled-intent runtime now support a first-class `live_action.enqueue_operator_send` path for operator-requested outbound sends, low-risk live-action validation now rejects send attempts with an explicit redirect instead of failing late, operator-directed sends are stamped with deterministic operator approval context before queueing, and focused compiled-intent regressions now prove sandbox group sends are actually queued as `send_group_message` actions.
- Implemented the first campaign-owned humanized engagement policy slice: added `telegram_app/engagement_policy/` for per-campaign quiet hours, reply-latency tiers, negative-signal suppression, and lightweight outcome metrics; wired the engagement brain to delay or suppress replies before queueing; taught follow-up timing to respect campaign quiet-hours policy; and fed live-execution outcomes back into compact campaign metrics with focused engagement-policy, timing, coordinator, review-dispatch, and execution tests.
- Added a one-day private Telegram sandbox runbook at `wiki/plan/telegram-live-sandbox-day-plan.md` so the team can validate the real Telethon-backed runtime, workers, review flow, and live-ops controls in a controlled environment before any wider beta.
- Added `bin/live-sandbox.ps1` as a small local launcher for sandbox roles (`poll`, `listener`, `scheduler`, `executor`, `reviewer`, and optional `api`), with isolated sandbox state/data/monitoring roots and automatic `TELEGRAM_CAPABILITY_BACKEND=telethon` wiring.

- Promoted managed-account “human presence” from ad hoc behavior into an explicit MTProto policy seam: typing now simulates roughly 3 characters per second, active read/send/list/leave actions open a 15-30 second online window before and after work, read receipts fire when dialog history is actually read instead of on every outbound send, and reactive replies still mark the thread read before responding.
- Expanded send failure handling for managed-account outbound messages: retriable send failures now allow three immediate retries, then one two-minute backoff window, then one final attempt before the send is aborted and recorded as failed.
- Added explicit human-presence handling for managed MTProto accounts: listener-connected accounts are pushed offline after attach, outbound sends now flip online only around active work, show a brief typing indicator, mark dialogs read before replying, and return offline afterward.
- Added focused MTProto and engagement-listener tests that prove online/offline toggling, typing-action signaling, read acknowledgements, and listener-side offline reset behavior.
- Extended outbound formatting hardening to managed-account MTProto sends: `telegram_app/capabilities/mtproto/impl_messaging.py` now renders the same Telegram-safe HTML formatting policy used by the Bot API transport and passes explicit Telethon `parse_mode="html"` instead of relying on library defaults.
- Added focused MTProto messaging regressions that prove managed-account sends and replies include the expected rendered payload plus explicit parse mode.
- Removed a major planning-surface capability bottleneck in the active runtime: added `telegram_app/llm/capability_tools.py` as a shared Anthropic tool-use loop for bounded Telegram read capabilities, wired the orchestrator plus discovery plus strategy plus account-planning surfaces through it, and updated the specialist prompts so they use direct Telegram tools when the runtime exposes them instead of treating compact injected context as the only live evidence path.
- Tightened runtime readiness visibility around Telegram access: `telegram_app/agent_runtime/broker.py` now distinguishes stubbed versus live-ready capability state, account-roster summaries no longer imply that stub accounts are production-ready, runtime context now exposes a first-class `telegram_capability_summary`, and planning responses append a deterministic operator-facing notice when live Telegram access is still stubbed or no managed accounts are onboarded.
- Added focused regression coverage in `tests/test_agent_capability_tooling.py` for the shared tool loop, stub-runtime readiness reporting, and the operator-facing stub-mode notice; also re-ran targeted planning-surface regressions from `tests/test_telegram_runtime_state.py` with a repo-local pytest `--basetemp` override to avoid the Windows temp-path permission issue.

## 2026-05-30

- Added targeted regression coverage around the cleaned control plane: `tests/test_server_runtime.py` now smoke-tests runtime composition plus worker factories, `tests/test_operator_notifications.py` now proves a direct operator schedule control can resolve a blocked continuous-ops loop through the compiled-intent path, and `tests/test_telegram_runtime_state.py` now covers mixed approval-plus-revision phrasing plus stricter activation detection so incidental mentions of activation do not trigger execution.
- Tightened `telegram_app/orchestrator/orchestrator.py` activation parsing so only explicit imperative activation requests are treated as plan activation, reducing false positives during normal review conversation.
- Verification: `python -m pytest --basetemp=.pytest-tmp-full tests/` passed with `227 passed`; the plain `python -m pytest tests/` invocation initially failed on Windows temp-directory permissions for pytest's default `tmp_path` root, so the repo-local `--basetemp` override was used for the full regression pass.
- Implemented the `wiki/plan/llm-led-runtime-integration/cleanup-and-cutover.md` slice in the active runtime: specialist advisory parsing now accepts only the shared `COMPILED_PROPOSALS_JSON` contract, orchestrator-authored schedule changes no longer depend on the retired `SCHEDULE_ACTION_JSON` marker path, planning review prompts plus follow-on routing now read persisted advisory proposals before falling back to compatibility defaults, and focused compiled-intent plus Telegram runtime plus engagement-brain tests cover the cleaned proposal-first control path.
- Implemented the `wiki/plan/llm-led-runtime-integration/agent-infra-and-tooling-expansion.md` slice in the live runtime: added a bounded `telegram_app/agent_runtime/` broker for shared read-side inspection, planning and observation prompts now receive active work plus active schedules plus compact readiness/pressure/traction/worker-health/proposal-outcome summaries by default, compiled intents now persist a first-class `blocked` lifecycle state with prompt-safe recent-outcome summaries, and focused compiled-intent plus continuous-ops plus runtime-context plus engagement-brain tests cover the new read-side seam without widening direct write access.
- Implemented the `wiki/plan/llm-led-runtime-integration/agent-architecture-transition.md` slice in the active runtime: the orchestrator now has an explicit reasoning-surface route model, prompt context now exposes `work_and_pressure_first` routing metadata plus reasoning-surface annotations for work items and schedules, new planning work families default conceptually to the shared campaign-planning surface, and repo-facing prompts/docs now describe the runtime primarily as `control brain + reasoning surfaces + compiled intents + late deterministic execution` while keeping the current discovery/strategy/account-planning flow compatible.
- Finished the remaining planned work for `wiki/plan/llm-led-runtime-integration/specialist-output-and-execution-transition.md`: discovery, strategy, and account-planning surfaces now support a shared `SPECIALIST_PROPOSALS_JSON` companion contract, the active runtime persists typed advisory planning proposals alongside durable artifacts, fallback proposal synthesis keeps older marker-only specialist outputs compatible, and focused compiled-intent plus end-to-end workflow tests now cover the proposal-first specialist path.
- Finished the next `llm-led-runtime-integration` specialist-output transition cut in code: planning revision reopen flows now persist `work.refresh` compiled intents instead of mutating work items directly, revised account plans now invalidate stale prepared execution through a typed `prepared_execution.invalidate_stale` intent path, runtime composition wires prepared-execution invalidation into the shared applicator seam, and focused compiled-intent plus prepared-execution tests cover the new transition behavior.
- Added `wiki/plan/llm-led-runtime-integration/agent-architecture-transition.md` and updated the integration index plus implementation sequence so the LLM-led migration series now has a dedicated architecture slice separate from the infra slice; the sequence now explicitly transitions the repo from a fixed planning-specialist ladder toward the intended `control brain + reasoning surfaces + compiled intents + late deterministic execution` model before expanding richer agent-runtime tooling.
- Added `wiki/plan/llm-led-runtime-integration/agent-infra-and-tooling-expansion.md` and updated the integration index plus implementation sequence so the LLM-led migration series now explicitly plans the next infra expansion step: richer bounded read access for reasoning surfaces, shared agent-runtime inspection seams, proposal-lifecycle visibility, and artifact-plus-proposal planning outputs without widening direct write power.
- Clarified the LLM-led source-of-truth docs around the intended final runtime shape: `wiki/spec/llm-led-outreach-runtime.md`, `wiki/spec/freeform-to-structured-compilation.md`, and the `wiki/plan/llm-led-runtime-integration/` docs now explicitly describe the end state as one operator-facing control brain plus bounded reasoning surfaces plus a shared compiled-intent seam plus late deterministic execution, with `discovery`, `strategy`, and `account_planning` treated as bounded planning work families rather than the permanent top-level runtime architecture; `wiki/index.md` and `wiki/code-index/index.md` now also reflect that direction.
- Implemented the `llm-led-runtime-integration` specialist-output-and-execution transition slice in code: specialist review-ready posture now persists through `review.request` compiled intents, observation review memory notes and planning refreshes now route through compiled-intent applicators, and promoted-thread review now records typed `engagement.next_move`, belief-state, and learning-note proposals before the existing authorization, policy, and queueing seams decide whether any outbound action is allowed.
- Implemented the `llm-led-runtime-integration` operator-control-and-work-proposal migration slice in code: campaign-context promotions and revision-resolution updates now persist through typed compiled intents, live-ops control intents now carry more structured voice and safeguard payloads, direct operator schedule control continues to bypass marker-only authoring, and planning review approval now creates or refreshes follow-on work through `work.propose` / `work.refresh` compiled intents instead of jumping straight through a hardcoded next-step path.
- Added focused coverage for the proposal-first control path: compiled-intent tests now distinguish create-if-missing versus stale-refresh follow-on planning behavior, live-ops tests now assert structured voice/safeguard payload persistence, and the broader specialist-workflow runtime test still passes with proposal-driven follow-on activation.
- Implemented the `llm-led-runtime-integration` intent-envelope-and-persistence slice in code: added `telegram_app/compiled_intents/` with typed compiled-intent records, campaign-scoped `compiled-intents.json` storage, per-kind validation, and narrow applicators for schedule, work-item, and memory-note mutations; runtime composition now wires that seam into the orchestrator; and prompt-authored recurring schedule actions now persist proposed/accepted/applied compiled intents before mutating schedule state, with focused compiled-intent and schedule-path tests.
- Added `wiki/plan/llm-led-runtime-integration/` as a new umbrella implementation series that converges the `llm-led-outreach-runtime` behavior spec with the `freeform-to-structured-compilation` control-plane spec, including notes for current-state audit, intent envelope and persistence, operator control and work proposal migration, specialist output transition, and recommended implementation sequence.
- Added an explicit cleanup-and-cutover path to that integration series so marker-first contracts, phrase-gated control parsing, and fixed planning-ladder migration scaffolding have a planned retirement path instead of silently becoming permanent architecture.
- Updated `wiki/index.md` and `wiki/plan/llm-led-outreach-runtime/index.md` so the new integration plan is discoverable from the main wiki and explicitly connected to the outreach-runtime behavior series.

## 2026-05-29

- Narrowed `wiki/spec/llm-led-outreach-runtime.md` and its immediate plan notes so the outreach series now owns behavioral and reasoning goals only, while freeform-expression, typed-intent compilation, and broader control-plane extensibility concerns are explicitly pushed into `wiki/spec/freeform-to-structured-compilation.md` instead of continuing to constrain the outreach spec with planning-era runtime assumptions.
- Added `wiki/spec/freeform-to-structured-compilation.md` to formalize the control-plane design principle behind the recent audit findings: agents should be free in cognition, the runtime should compile freeform reasoning into typed intent records, and deterministic seams should remain responsible for durable mutation, authorization, and execution rather than letting freeform output act directly on the world.
- Refreshed `wiki/plan/llm-led-outreach-runtime/current-state-audit.md` to record the latest spec-alignment audit: the doc now reflects that cheap inbound triage, conversation belief state, promoted-thread commercial reasoning, and commercial traction summaries are already landed, and it captures the main remaining contradictions as phrase-gated operator control, scheduled-post-centric activation contracts, and stale neighboring readiness docs.
- Refined `wiki/spec/llm-led-outreach-runtime.md` so the product direction is now explicit about the intended autonomy boundary: LLMs should interpret campaign strategy, operator steering, launch timing, tone, safeguards, and proactive group-outreach decisions; deterministic code should stay focused on enforcement, consent, queue discipline, and execution safety; proactive group posting is now first-class autonomous behavior; outbound DMs remain inbound-first; and runtime readiness now includes real backend plus worker health instead of only campaign-control completeness.

## 2026-05-27

- Implemented the `llm-led-outreach-runtime` opportunity-signals and commercial-summaries slice in code: campaign signals now classify persisted facts as `risk`, `opportunity`, or `yield`; live conversation and qualification flows now emit positive commercial signals such as public-to-DM transitions, pricing interest, clarified need, conversion-ready threads, and delivered handoffs; continuous ops now persists compact traction counts plus hotspot summaries alongside loop readiness; and live ops status now surfaces traction, commercially meaningful attention items, and momentum hotspots with focused signal, qualification, continuous-ops, live-ops, and handoff-path tests.
- Implemented the `llm-led-outreach-runtime` promoted-thread commercial reasoning slice in code: `telegram_app/engagement_brain/service.py` now splits promoted-thread commercial review from bounded draft generation, higher-capability review can use a dedicated `commercial_reasoning` model role plus `prompts/live_engagement_review.md`, the coordinator now persists review-owned belief-state updates before autonomous-send and live-execution checks, and focused engagement-brain plus qualification plus review-dispatch tests cover the new seam without changing deterministic authorization or queue policy.
- Landed the `llm-led-outreach-runtime` evidence-foundation slice in the live engagement path: outbound reply-matching records now persist message text plus lightweight asset refs plus campaign/conversation linkage, live execution forwards queued asset refs into approved-send context, and engagement-brain context rebuilding now resolves recent outbound messages with real text and timing instead of empty placeholders.
- Added `wiki/plan/llm-led-outreach-runtime/` as the implementation series for the new outreach direction, including a current-state audit, implementation sequence, and five explicit slices covering evidence foundation, cheap inbound triage, conversation belief state, promoted-thread commercial reasoning, and opportunity plus commercial-summary work.
- Refined `wiki/spec/llm-led-outreach-runtime.md` so the target live architecture now explicitly includes a two-stage model pipeline: a cheaper bulk inbound-read layer for lightweight interest and review signals, followed by selective promotion into higher-capability commercial reasoning for belief-state updates, next-move decisions, and outbound-adjacent judgment.
- Added `wiki/spec/llm-led-outreach-runtime.md` and updated the wiki/code-index navigation so the desired product direction is explicit: the app should behave as an LLM-led outreach machine where adaptive reasoning owns conversation interpretation, prioritization, and qualification, while deterministic runtime seams stay focused on evidence integrity, policy enforcement, execution discipline, and operator-visible control.
- Added a no-Docker deployment kit under `deploy/no-docker/` with a quick-start runbook, a main `systemd` service, an optional scheduler-worker service, and an Nginx reverse-proxy template; `README.md` now also points server deploys toward that direct Python path instead of requiring Docker-first setup.
- Added a deployment-friendly monitoring surface on top of the existing runtime event seam: `telegram_app/monitoring/` now mirrors structured events into both JSONL and SQLite, `server.py` composes that monitor into the app service plus orchestrator by default, webhook delivery now records completed/skipped/failed transport events, and FastAPI now exposes authenticated `/ops/monitoring/status`, `/ops/monitoring/summary`, `/ops/monitoring/events`, and `/metrics` endpoints so the same historical runtime data can be inspected locally or remotely on the server.
- Enriched that monitoring surface with derived operational metrics and threshold-backed alerting: summaries now include failure rate, throughput, unique trace/session/chat/campaign counts, grouped Telegram delivery and app-turn counters, FastAPI now exposes `/ops/monitoring/alerts`, Prometheus export now includes health and alert-state gauges, and threshold values are configurable through `TG_SWARM_MONITORING_MAX_*` environment settings.
- Added broader Telegram-native outreach regression coverage: multi-account prepared-wave activation now runs through live queue dispatch and inbound reply projection, mid-wave rate-limit and flagged-account scenarios prove other eligible accounts can keep moving, restart/claim-expiry tests prove queued actions are not lost or double-sent, pause-after-claim tests prove campaign stops still block late sends, quiet-hours follow-up tests now cover overdue windows that are rescheduled from actual send time, and MTProto send-edge tests now cover long-text passthrough plus `peer_invalid`, `message_not_found`, and `write_forbidden` outcomes; the live-execution policy/runtime was also tightened to block stale conversation approval context and mismatched stored reply targets before any outbound reply is sent.
- Reworked the live reply path so `telegram_app/engagement_brain/` now separates deterministic reply-mode decisions from bounded draft generation, using a structured voice profile plus approved-claims plus forbidden-claims plus community-guidance contract instead of fixed reply templates.
- Tightened live-risk handling across the active runtime: community risk is now normalized as `low|guarded|high|restricted`, conversation risk is now normalized as `low|needs_clarification|sensitive|high_stakes`, and those labels now flow through engagement-brain context, proposal records, and autonomous approval context.
- Fixed autonomous-send posture enforcement so `manual_only` reply modes now create durable review-needed records instead of silently allowing supported replies to flow straight into execution.
- Added deterministic approval-context enforcement in `telegram_app/live_execution/policy.py` so malformed, mismatched, or posture-violating autonomous sends are blocked before capability dispatch even if something upstream mis-queues them.
- Updated the strategy prompt, live-engagement prompt, focused tests, and code-index notes so the active Telegram-native runtime documents the new split between generation-time flexibility and execution-time determinism.
## 2026-05-30

- Added `wiki/plan/agentic-campaign-runtime/canonical-campaign-spec-and-revisions.md` and updated the folder index plus implementation sequence to record a simpler runtime direction: one canonical campaign spec as the source of truth, derived discovery/strategy/account-plan views, one pinned active revision for execution, operator-approved initial runnable revisions, and autonomous operational revisions that notify the operator instead of blocking on approval for every live tuning change.
- Implemented the `agentic-campaign-runtime` operator-notifications-and-recovery slice in code: added `telegram_app/operator_notifications/` so campaign intervention events are derived from continuous-ops plus live risk state, persisted with delivery plus acknowledgement plus resolution tracking, surfaced back through ordinary Telegram turns, and exposed through compact `show alerts` plus `ack alerts` recovery hooks with focused runtime tests.
- Landed the `agentic-campaign-runtime` continuous-autonomous-operations slice in code: added `telegram_app/continuous_ops/` so each campaign now persists one durable autonomy summary derived from campaign intent, work items, schedules, signals, and observation review; operator turns, scheduled work, and live signal ingestion now refresh that state; runtime prompt context includes the continuous-ops summary; and campaign memory now renders loop status plus blockers into `overview.md` and `next-actions.md` with focused tests for blocked, running, and operator-attention states.
- Landed the `agentic-campaign-runtime` qualification-and-handoff slice in code: added `telegram_app/qualification/` for campaign-owned qualification-frame persistence plus handoff outcome updates, extended live conversation records with durable qualification and handoff fields, taught the engagement brain to persist qualification readiness and use conversion-target-aware handoff hints, and wired live execution to mark handoff deliveries versus blocked or failed routing attempts back into campaign memory and focused tests.
- Landed the `agentic-campaign-runtime` conversion-target-contract slice in code: `telegram_app/conversion_target.py` now turns campaign-intent signals into a durable normalized `conversion_target` artifact, intake and workflow snapshots persist plus expose that contract alongside the existing compatibility brief, campaign memory now syncs `artifacts/conversion_target.json` plus operator-facing summaries into workspace files, runtime prompt context includes `conversion_target_summary` and `conversion_target_data`, and focused runtime-state tests cover Telegram DM and external website targets across intake, context, and campaign-memory hydration.
- Landed the `agentic-campaign-runtime` asset-role-inference slice in code: campaign assets now persist additive inferred roles plus conservative uncertainty notes in `assets/manifest.json`, the analyzer promotes stored docs and images into prompt-safe multi-role summaries for orchestrator context, operator-facing upload acknowledgements now surface uncertainty when asset use is still unclear, and runtime-state tests cover both conservative context-only inference and richer multi-role inference without changing explicit sendable-label compatibility.
- Implemented the first `agentic-campaign-runtime` intake slice in code: `telegram_app/campaign_intent.py` now synthesizes a durable `campaign_intent` artifact from mixed operator input, `StructuredIntakeCoordinator` now persists that artifact before projecting compatibility `campaign_brief` and `campaign_setup_state` views, runtime prompt context now exposes `campaign_intent_summary` plus `campaign_intent_data`, campaign memory sync now persists and renders the new artifact seam, and intake/app-service tests now cover seed extraction, conversion-target signal capture, and compatibility preservation across follow-up acknowledgement turns.
- Enriched `wiki/plan/agentic-campaign-runtime/campaign-intake-and-synthesis.md` into an implementation-ready Step 1 plan by locking the MVP campaign-intent-package contract, clarifying source-of-truth versus compatibility boundaries, defining synchronous versus deferred interpretation, standardizing the operator-facing interpretation summary, and adding explicit acceptance criteria plus non-goals for the slice.
- Added `wiki/spec/agentic-campaign-runtime.md` as the new highest-priority product-direction spec, defining the shift toward mixed-input campaign interpretation, orchestrator-led multi-role asset inference, first-class conversion targets, campaign-specific qualification, and continuous autonomous operation with operator escalation.
- Added `wiki/plan/agentic-campaign-runtime/` as a new scaffolded implementation series with notes for current-state audit, campaign intake and synthesis, asset-role inference, conversion target contract, qualification and handoff runtime, continuous autonomous operations, operator notifications and recovery, and recommended implementation sequence.
- Updated `wiki/index.md` and `wiki/code-index/index.md` so the new agentic campaign runtime direction is visible from the main wiki and code-navigation entrypoints.
- Added a first-class `telegram_app/campaign_context.py` seam to the active Telegram-native runtime so operator preferences, revision intent, campaign voice guidance, execution constraints, persistent decisions, and open ambiguities are promoted into one durable structured artifact instead of relying on recent chat history or specialist-local notes.
- Wired campaign-context promotion through the real active path: intake now seeds the new artifact, review-turn revisions promote and later resolve structured revision threads, `build_runtime_context()` now injects compact prompt-safe campaign-context data for the orchestrator and downstream specialists, and campaign-memory rendering now reflects the promoted context back into `operator-intent.md` and `personas.md`.
- Added focused regression coverage proving nuanced revision guidance survives across turns, persists through session reload, and reaches follow-on specialists without depending on raw conversational recency; also updated the top-level code index so the new campaign-context seam is part of the documented active runtime map.
- Cut over the supported live reply path away from operator message review: `telegram_app/autonomous_send/` now stamps structured autonomous approval for grounded DM and group replies by default, retires stale pending review records for those replies, `telegram_app/engagement_brain/coordinator.py` no longer emits a `review_required` branch for reply-path sends, and MTProto visible sends now require structured operator or autonomous approval context instead of trusting a bare boolean.
- Updated the engagement-machine-readiness docs and code-index notes so the repo now describes the live reply path accurately: no per-message operator review for supported sends, live ops should focus on pause/resume plus inspection, and structured approval remains mandatory at the MTProto edge.

## 2026-05-25

- Added `wiki/plan/engagement-machine-readiness/next-session-autonomous-send-cutover-prompt.md` to hand the next coding session a concrete override: remove operator review from supported live message sends, let system-approved grounded messages flow directly into execution, and preserve policy plus MTProto approval gates.
- Expanded `wiki/plan/engagement-machine-readiness/telegram-live-ops-surface.md` from a short readiness stub into an implementation-ready operator-surface plan: it now locks the Telegram-native live-ops direction, scopes the first cut to the session-attached campaign, defines campaign status and attention-queue contracts, specifies pause/resume and autonomous-review resolution flows, proposes a dedicated `telegram_app/live_ops/` seam, and names the file-level runtime and test slices needed to land the operator control surface cleanly.

## 2026-05-30

- Migrated the first operator-control slice onto the compiled-intent seam: the orchestrator now compiles direct live-ops controls and narrow recurring-schedule requests from ordinary operator chat before mutating runtime state, persists those proposals in `compiled-intents.json`, and applies accepted voice/safeguard/pause/resume/posture/review and schedule intents through the existing deterministic managers instead of relying primarily on phrase-only or marker-only paths.
- Moved general campaign-context promotion onto a typed `campaign_control.update_context` proposal path during operator turns, so durable nuance updates now leave an inspectable compiled-intent record even while the existing campaign-context artifact remains the application target during transition.
- Replaced the hardcoded planning follow-on mutation path with persisted `work.refresh` proposals on review acceptance, keeping the current discovery -> strategy -> account-planning behavior while making the next-work mutation typed, inspectable, and reusable by deterministic evaluation.
- Extended the compiled-intent compiler, validator, and applicator stack to cover live-ops control kinds, direct work refresh metadata, and campaign-context update handling, and added focused coverage for multi-intent live-ops control, direct schedule compilation without an LLM marker block, and compiled follow-on work persistence.
- Verification: `python -m pytest -q --basetemp=.pytest-tmp3 tests/test_compiled_intents.py tests/test_live_ops.py` and `python -m pytest -q --basetemp=.pytest-tmp3 tests/test_telegram_runtime_state.py -k full_specialist_workflow`.

## 2026-05-13

- Wired the remaining schedule-authoring gap into the live runtime: the orchestrator can now author recurring schedule actions during normal operator turns, the runtime validates and persists `create` / `pause` / `resume` actions from a machine-readable schedule appendix, and schedule persistence now exposes the minimal lookup and status-update helpers needed for that path.
- Added `wiki/spec/managed-account-operations.md` to define the agentic-first operational loop for managed MTProto work as `prepare -> execute -> observe -> adapt`, aligned to operator direction, orchestrator coordination, specialist tactical autonomy, approval-aware execution, and durable campaign-memory feedback.
- Finished the next campaign-memory integration slice: strategy and account-planning specialists now ingest canonical campaign memory plus their own local working-memory files, campaign sync preserves specialist-owned `agents/*.md` notes instead of regenerating them from artifacts, and the active specialists now rewrite their own tactical memory deliberately after successful runs.
- Reduced the live runtime's dependence on workflow stages by making `PurposeBuiltOrchestrator` resolve specialist turns from the primary campaign work item first, backfilling stage-only sessions into compatibility work items when needed, and passing bounded work-item goals into specialist runs while keeping workflow snapshots as operator-facing compatibility views.
- Realigned `wiki/spec/account-capability.md` to match the original managed-account capability grouping explicitly: `presence`, `messaging`, `engagement`, `dialog`, `identity`, and `social`, with basic message send now living clearly under `messaging`.
- Tightened `wiki/spec/account-capability.md` so the account-focused MTProto surface explicitly includes basic outbound message send as a first-class consequential write alongside presence, read-state actions, reactions, identity changes, and other user-like account behaviors.
- Added `wiki/spec/account-capability.md` to define the narrower account-focused MTProto surface under the broader Telegram capability layer, including inventory/health, presence, read-state actions, lightweight engagement like reactions, identity management, approval expectations, and rollout guidance for making managed accounts behave more like real users over time.
- Completed the next campaign-memory and operations cut: added `telegram_app/campaign_memory/` for full workspace bootstrapping, compatibility artifact views, prompt-safe memory loading, and session hydration; expanded campaign workspaces to create the broader Markdown and agent-memory surface; shifted scheduled work to build campaign-native background context without requiring a preexisting session; and tightened orchestrator review routing so the primary open work item now outranks raw artifact presence when deciding whether to refresh work or enter review.
- Updated the scheduler-related runtime docs to match the current implementation: the app-runtime architecture spec and campaign-operations runtime plan now describe the dedicated `server.py --run-scheduler` worker, lease-protected due-schedule dispatch, scheduled specialist execution instead of placeholder-only work-item creation, simple schedule health thresholds with auto-pause, and the current latest-session bridge used while some artifacts still remain session-scoped.
## 2026-05-27

- Centralized Anthropic model selection under `telegram_app/llm/model_selection.py`, kept Sonnet as the default planning model, added a separate `SUMMARY_MODEL` override with Haiku as the summary fallback, and switched bounded observation review onto that cheaper summary-model path without changing the deterministic Telegram read or signal-detection seams.
- Landed the cheap inbound triage slice for live conversation review: added `telegram_app/engagement_triage/` with a summary-tier prompt contract plus structured triage parsing/fallbacks, persisted compact triage state on external conversation records, and taught `ConversationReviewDispatcher` to short-circuit low-signal threads while promoting higher-value inbound moments into the existing deeper engagement-brain path.
- Implemented the conversation-belief-state runtime slice: external conversation records now persist a nested `belief_state` alongside the existing `triage_state`, cheap triage now preserves objection hints plus an explicit promoted flag, qualification review writes accumulated commercial meaning back into the conversation record, and the engagement-brain context builder now folds that structured state back into promoted-thread review context without removing the current compatibility summaries.

- Completed the first operational cut of recurring scheduling: due schedules now execute specialist work instead of only creating placeholder items, schedules track outcome health and can auto-pause after repeated misses, session lookup now resolves campaign context for background runs, and `server.py --run-scheduler` starts a dedicated lease-protected scheduler worker so webhook, polling, and multi-instance app processes do not all dispatch the same due work by default.
- Landed the next campaign-runtime slices in code without a flag-day rewrite: added durable `telegram_app/work_items/` and `telegram_app/scheduling/` packages, introduced work-item and schedule runtime models, taught the orchestrator to track discovery/strategy/account-planning work items through creation, review, revision, completion, and schedule-triggered refresh, and now explicitly cancel legacy planning approvals when old `waiting_for_approval` state is released back into conversational review.
- Landed the first campaign-runtime cutover slice in code: added `telegram_app/campaigns/` for file-backed campaign workspace bootstrapping plus `campaign.json` metadata, gave sessions first-class `campaign_id` and `campaign_workspace_path` fields, taught `TelegramAppService` to auto-attach or backfill campaigns and sync the intake objective into campaign metadata, and updated the runtime context plus code index to expose the new campaign seam without removing the existing stage-driven flow yet.
- Synced the remaining older platform specs to the campaign-operations model: `wiki/spec/telegram-core-platform.md` now describes campaigns, delegated work, and recurring operating cadence instead of a session-only workflow story, and `wiki/spec/telegram-capability-layer.md` now explicitly supports campaign-attached work items, schedule-driven reads, and approval-aware write routing.

## 2026-05-12

- Tightened the campaign-operations and campaign-memory docs so the state model is unambiguous: campaigns are now the durable root object for downstream records, sessions are explicitly only the operator interaction layer, schedule-triggered work resolves campaign context directly, `active_session_id` and campaign-level stage/mode fields were removed from the recommended memory contract, and the runtime-refactor plan now includes an explicit migration phase to remove legacy planning approval state instead of letting it survive by accident.
- Added `wiki/plan/campaign-operations-runtime-refactor.md` and `wiki/plan/campaign-memory-implementation.md` as the first implementation plans directly subordinate to `wiki/spec/campaign-operations-model.md`: the runtime-refactor plan covers campaign-aware orchestration, work items, scheduling, and bounded specialist autonomy, while the memory plan covers campaign workspace layout, shared versus agent-local memory, lightweight metadata, and migration from the current artifact model.
- Updated the existing runtime, session, marketing-mode, and approval specs to align with the new campaign-operations model: sessions are now described as campaign-attached conversational threads, the runtime now targets campaign memory plus work-item and scheduling support, the marketing MVP is framed as an ongoing operating system rather than only a one-pass workflow, and approvals are now scoped more explicitly around consequential execution instead of normal planning or recurring maintenance work.
- Added `wiki/spec/campaign-operations-model.md` as the new architecture-level source of truth for the operating model shift from a consecutive workflow into a campaign operations system: operator as manager, orchestrator as campaign manager, specialists as tactically autonomous workers, campaigns as durable workspaces, work items plus schedules as the main coordination primitives, and shared campaign memory plus agent working memory as the long-lived continuity model.
- Added `wiki/plan/campaign-memory-redesign.md` to capture the next storage-direction decision: keep runtime state small and structured, but move campaign planning and long-lived marketing knowledge toward a flexible file-backed workspace with Markdown-first memory and lightweight campaign metadata instead of deepening the current fixed-artifact JSON model.
- Implemented persistent Telegram polling cursor state so long-poll restarts do not replay the same inbound Bot API update: `telegram_app/polling_runner.py` now restores and saves the last successfully delivered `update_id`, skips already-processed duplicates, and only advances the cursor after successful response delivery; focused polling tests now cover retry, restore, persistence, and duplicate-skip behavior.
- Hardened the local Telegram polling runner for live testing: `telegram_app/polling_runner.py` now retries `deleteWebhook` and `getUpdates` after transient Bot API failures instead of letting the whole poller die, and `tests/test_polling_runner.py` now covers both retry paths.
- Cleaned up `wiki/plan/target-search-refinement.md` so the “shipped” status and remaining-work sections now align with the actual discovery runtime: stale pre-build gap language was rewritten as historical context already closed by the current build.
- Finished the remaining target-search refinement code slices: discovery now persists explicit harvest-versus-validation diagnostics, shortlist communities carry compact `evidence_summary` and `validation_path` evidence, exact live validation now wins ranking ties over broader harvest matches, and operator/downstream shortlist summaries now explain sparse coverage and fallback reliance without passing raw search traces forward.
- Implemented the broader MTProto search fallback from the target-search plan: `CommunityCapabilityImpl.search()` now uses `messages.SearchGlobalRequest` when `harvest` mode gets sparse `contacts.search` results, merges and deduplicates global chats behind the existing bounded flow, preserves per-result `search_source`, and has focused MTProto tests covering both the fallback and exact-mode no-fallback path.
- Implemented the bounded refinement slice from the target-search plan: discovery now runs one deterministic second-pass harvest only when first-pass live coverage is sparse, persists refinement diagnostics including productive families and added candidates, and has focused runtime tests covering the trigger and bounded behavior.
- Expanded the wiki support around target-search refinement: `wiki/plan/target-search-refinement.md` now distinguishes shipped slices from remaining work, `wiki/code-index/discovery-search.md` maps the live discovery-search path and boundaries, and `wiki/code-index/index.md` now points directly at that narrow search map.
- Implemented the next target-search refinement slice in code: `CommunityCapability.search()` now accepts explicit `mode` and `limit` parameters, the MTProto and stub community capabilities preserve those search semantics in their results, discovery brief searches now run in `harvest` mode with a larger limit, and shortlist enrichment now tries the already-harvested live candidate pool before issuing fresh exact-match requeries.
- Implemented the first code slice of target-search refinement in discovery: brief search generation now uses a broader deterministic query budget with related-phrase and city-hub variants, the LLM now receives a compact `community_search_summary` instead of raw per-query search dumps, and persisted shortlist artifacts now carry `search_diagnostics` with per-query coverage metrics while downstream strategy/account-planning prompt payloads continue to exclude those bulky diagnostics.
- Rebuilt `wiki/plan/target-search-refinement.md` into a tighter implementation plan aligned to the current discovery runtime: it now captures the shipped baseline, the remaining search-coverage gaps, phased file-level work, acceptance criteria, non-goals, and a recommended delivery order for broader live Telegram discovery without prompt-context bloat.
- Tightened `wiki/plan/target-search-refinement.md` to make the build-critical decisions explicit without expanding scope: compact discovery prompt summaries must land before broader query execution, prompt-contract updates are part of the implementation track, public `verification_state` labels stay stable for this build, and new lookup references should be added with temporary backward compatibility for downstream consumers.
- Added token-efficiency guardrails to `wiki/plan/target-search-refinement.md`, documenting the main prompt-spend risks in the current discovery/search flow and the design constraints needed to keep broader search logic from bloating model context.
- Added a bounded query-refinement loop to `wiki/plan/target-search-refinement.md`: after the deterministic first pass, discovery may run one tightly constrained second pass based only on query families that already produced useful live candidates.
- Added a plain-language explanation section to `wiki/plan/target-search-refinement.md` so the search-expansion strategy, staged validation model, evidence-preservation rationale, and accuracy guardrails are understandable without reading the implementation details alone.
# 2026-06-01

- Added repo-native Telegram sales drafting skill assets under `prompts/sales_skills/`, covering outbound DM drafts, follow-up drafts, objection replies, and first-post public group outbound guidance tuned for `tg-swarm` runtime concepts such as `voice_profile`, `approved_claims`, `response_posture`, and community risk posture.
- Kept those drafting assets strictly Telegram-native, removed meta coaching language aimed at human operators, and allowed controlled soft DM/channel CTA variants only in the public group outbound skill when promo tolerance supports them.
- Added a bounded live drafting-skill selector seam under `telegram_app/engagement_brain/drafting_skills.py`, so copy-writing surfaces can use one LLM-chosen Telegram-native skill packet plus an optional small secondary packet during reply drafting while keeping runtime grounding, community guidance, and policy constraints higher priority.

- Expanded `wiki/plan/target-search-refinement.md` with MTProto-specific search constraints and a staged solution shape, including deterministic query-family expansion, hybrid `contacts.search` plus `messages.searchGlobal` harvesting, stable lookup-reference preservation, and first-class search diagnostics.
- Added `wiki/plan/target-search-refinement.md` to capture discovery-search issues found in live review, including brittle brief query generation, shallow Telethon search depth, validation-first search flow, and permissive approximate matching of training-seeded candidates to live Telegram entities.
- Removed hard runtime approval gates from the discovery, strategy, and account-planning flow. The runtime now keeps those checkpoints conversational: specialists still ask the operator whether to continue or revise, but the session no longer blocks on persisted `waiting_for_approval` state for planning artifacts.
- Updated orchestrator routing and prompts so shortlist, strategy, and account-plan follow-ups now progress or revise within normal chat turns, while legacy pending-approval sessions are released back into the active conversational stage.

## 2026-05-11

- Rewired `server.py` startup composition so `create_telegram_app_service()` now builds the Telegram capability bundle from `TELEGRAM_CAPABILITY_BACKEND`, injects stub or Telethon-backed account/community/membership/messaging capabilities into the orchestrator, wires `AuthManager` plus account inventory into `TelegramAppService`, and restores live `/accounts` plus `/addaccount` behavior for the Telethon runtime.
- Expanded `wiki/plan/workflow-refinement.md` to overlap the current runtime findings with prompt-contract drift, add a concrete prompt rework track, and document recovery instructions for null-byte corruption in `telegram_app/orchestrator/orchestrator.py` and other critical working-tree files.
- Fixed discovery-to-strategy evidence continuity by preferring matched usernames for live profile lookups during shortlist enrichment and by reusing persisted discovery profile snapshots in strategy before attempting fresh live reads.
- Added explicit shortlist verification state handling so discovery now persists `live_confirmed`, `search_confirmed`, and `training_knowledge_fallback` labels with verification counts/summary, re-ranks using that signal, and passes the verification data through strategy/account-planning prompt context.
- Renamed the npm launcher path from `bin/openswarm` to `bin/tg-swarm`, updated package metadata and lockfile to publish `tg-swarm`, and refreshed the macOS smoke-test workflow plus issue template labels to match the new package identity.
- Updated `run_utils.py` and `onboard.py` branding to `tg-swarm` while keeping fallback support for legacy `OPENSWARM_*` environment variables during the transition.

## 2026-05-30

- Unified planning, schedule-authoring, and promoted-thread review outputs behind one shared `COMPILED_PROPOSALS_JSON` contract while preserving legacy marker fallbacks during the transition.
- Added a generic output-proposal compiler so prompt-emitted proposal lists can compile into typed runtime intents across schedule, planning, memory, belief-state, and engagement-next-move surfaces.
- Updated the orchestrator to consume shared proposal lists for recurring schedule mutations before falling back to legacy `SCHEDULE_ACTION_JSON`, keeping deterministic schedule application unchanged.
- Updated discovery, strategy, and account-planning agents to parse the shared proposal marker and retain their existing durable artifact persistence.
- Extended promoted-thread commercial review parsing so review outputs can carry typed proposal payloads, while the coordinator still enriches those proposals with deterministic draft and authorization inputs before any late execution decision.
- Added focused regression coverage for the shared proposal compiler, the new orchestrator schedule contract, and promoted-thread review proposal parsing; verified with `python -m pytest -q --basetemp=.pytest-tmp-proposals tests/test_compiled_intents.py tests/test_engagement_brain.py tests/test_telegram_runtime_state.py`.

## 2026-05-31

- Expanded specialist read-side Telegram access so discovery and strategy now build the same broader live tool palette already available to the orchestrator: account inventory reads, membership reads, message reads, and recent-dialog/history reads are now wired into those planning surfaces instead of staying account-planning-only.
- Added a deterministic compiled-intent path for bounded low-risk Telegram actions: orchestrator outputs can now persist and apply `live_action.enqueue_low_risk` proposals, which enqueue `join_community`, `mark_read`, and `leave_dialog` actions into `telegram_app/live_execution/` without opening raw prompt-to-write execution.
- Added a shared MTProto warmup seam in `telegram_app/capabilities/mtproto/warmup.py`, surfaced warmup day/stage into prompt-safe account summaries and `/accounts`, and taught live-execution policy to cool down only the affected account/action class when warmup budgets are exhausted.
- Shifted campaign runtime defaults toward autonomous operation by making continuous autonomy the default effective loop mode, allowing reviewable signals to keep the loop running even without an existing schedule yet, and defaulting the stored outbound posture toward autonomous operation where the current runtime already has policy-mediated seams.
- Added bounded non-mechanical pacing in live execution with deterministic retry jitter and non-uniform ready-queue tie-breaking, while keeping Telegram-explicit `wait_seconds` exact and preserving deterministic auditability.
- Expanded live-ops tone parsing and prompt guidance so operator-controlled voice policy can more directly express Telegram-native constraints such as concise short-chat phrasing, less polished prose, less corporate filler, and lower-pressure service messaging.
- Fixed Bot API outbound message delivery so operator-facing Telegram replies are sent with `parse_mode="HTML"` instead of raw Markdown-like text.
- Added a small transport formatter that converts common Markdown-lite patterns such as inline code, fenced code blocks, and bold text into Telegram-safe HTML.
- Added focused tests for the formatter and Bot API payload construction so formatting regressions are caught without needing live Telegram delivery.

## 2026-05-10 (session 2)

- Normalized git remotes so `origin` points at `xts-x-xvxl-wxrld/tg-swarm` and `upstream` points at `VRSEN/OpenSwarm`, then reset `main` to track `origin/main`.
- Rewrote `AGENTS.md`, `README.md`, and selected metadata files so the repo describes the current Telegram-native runtime instead of the old OpenSwarm / Agency Swarm topology.
- Updated the code index to stop pointing at deleted `swarm.py`-style entrypoints and to reflect the purpose-built orchestrator path.
- Audited existing Agency Swarm infrastructure for reusability. Prompts (orchestrator, deep_research, virtual_assistant instructions) are high-value and framework-agnostic. Tool business logic is extractable with BaseTool wrapper removed. Framework glue (agent factories, swarm.py, patches) gets discarded entirely.
- Decided on total replacement of Agency Swarm rather than retrofit. Key reason: framework runs via LiteLLM compatibility shim on Claude - no Assistants API benefits apply, only overhead.
- Added `wiki/plan/agency-swarm-replacement.md` covering Phase 2 (replace Agency Swarm core), Phase 3 (build three specialist agents), and Phase 4 (implement Telegram capability layer with MTProto).

## 2026-05-10

- Added repository-level pytest configuration in `pyproject.toml` so tests resolve the repo root consistently and `pytest-asyncio` uses an explicit fixture loop scope.
- Prepared the Telegram runtime refactor work for a clean Git commit and push.

## 2026-05-09

- Created initial wiki structure for the Telegram marketing swarm effort.
- Added an MVP spec focused on discovery, strategy, and account management.
- Added an implementation plan for reshaping OpenSwarm into a narrower Telegram-focused harness.
- Reframed the product as a Telegram-native autonomous agent platform rather than a marketing-only swarm.
- Added a Telegram core platform spec covering the minimal bot UI, orchestrator, shared Telegram capability layer, and role-based autonomy.
- Updated the marketing spec so it now describes the first operating mode on top of the broader Telegram platform.
- Updated the implementation plan to build Telegram core concepts before workflow specialization.
- Added scaffolded design specs for app runtime architecture, Telegram capability boundaries, session lifecycle, and approval/guardrail design.
- Added an initial `wiki/code-index/index.md` so future implementation work has a human-readable code navigation entrypoint.
- Updated the wiki index to expose the new architecture-oriented design documents.
- Added a Phase 1 runtime refactor plan that maps the Telegram-native architecture onto concrete repo modules, boundaries, and sequencing.
- Scaffolded the `telegram_app/` runtime package with transport models, session and approval contracts, in-memory stores, capability interfaces, and runtime record models.
- Updated the code index so the new Telegram runtime package is part of the documented code navigation map.
- Added a thin `telegram_app/app_service.py` adapter and updated the runtime docs so the app layer stays focused on transport/session plumbing while the orchestrator interprets clarifications and approval replies.
- Wired a first end-to-end Telegram turn path: `server.py` now exposes a Telegram webhook, `telegram_app/orchestrator_adapter.py` routes turns into the agency orchestrator, session history is preserved in-memory, and the default swarm is narrowed to orchestrator plus deep research.
- Added a Telegram Bot API transport client, wired outbound `sendMessage` delivery into the webhook flow, added `/start` handling, and exposed webhook management endpoints for setup and inspection.
- Added a local long-polling runner and `python server.py --poll` mode so the real Telegram bot can be tested live without deploying a public webhook endpoint.
- Replaced the Telegram runtime's in-memory session and approval stores with local JSON-backed persistence, added structured workflow snapshot/artifact helpers to the session layer, wired the persistent stores into `server.py`, and added focused runtime persistence tests.
- Added a structured intake coordinator that turns operator turns into a persisted `campaign_brief` artifact, updates `workflow_snapshot` stages from intake to discovery, and passes the structured brief/snapshot context into the orchestrator at runtime.
- Added discovery-stage runtime helpers that request a machine-readable community shortlist from the research path, persist that shortlist as a `community_shortlist` artifact, create a pending approval, and move the snapshot to `waiting_for_approval`.
- Added a dedicated `telegram_app/live_ops/` seam for operator-facing Telegram control of live execution, including natural-language intent detection for status, blocked-state inspection, pause/resume, autonomous posture changes, tone and safeguard updates, pending-review resolution, and compact control-completeness reporting for the session-attached campaign.
- Extended `telegram_app/autonomous_send/service.py` so pending autonomous reviews can now be approved or dismissed deterministically from chat, with operator approval context preserved on queued live actions and stale conversation-linked review state cleared safely.
- Taught `telegram_app/engagement_brain/context_builder.py` to overlay durable live-ops control overrides, so operator-managed voice and safeguard changes survive across sessions and influence bounded live reply generation instead of staying trapped in recent chat history.
- Added focused live-ops tests plus a small shared `tests/conftest.py` Anthropics stub so the new operator-chat control surface, review resolution path, readiness reporting, and engagement-brain override propagation can be validated consistently without a local Anthropic install.
# 2026-05-31

- Hardened live Telegram send idempotency: visible MTProto sends now make one transport attempt, verify ambiguous transient failures against recent outbound history when possible, and otherwise fail closed instead of auto-retrying into duplicate public posts.
- Updated live execution handling so `transient_error` on visible outbound sends no longer re-enters queue retry; stale sandbox send action `50feefe6-feb4-4a68-84e9-f0689c56d2ca` was cancelled during validation after duplicate-send testing.
- Added explicit target-kind guards for live Telegram testing: broadcast channels are now distinguished from groups/supergroups and blocked with clear deferred-in-this-version outcomes for `join_community` and `send_group_message`.
